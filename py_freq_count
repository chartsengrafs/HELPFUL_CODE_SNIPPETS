#Functions
def top_most_common (df, num):
    from collections import Counter
    #Count the categories
    type_count = Counter(df)
    # Find the n most common elements
    print(pd.DataFrame(type_count.most_common(num),  
                       columns=['Category', 'Frequency']))
